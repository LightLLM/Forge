# goal-mvp fixture

Used by M11 goal mode gate tests.

- `SPEC.md` — objective description
- `plan.json` — multi-task DAG (parallel alpha/beta, then combine)

Run: `forge goal run "Build the MVP from plan.json" --workspace fixtures/goal-mvp --plan plan.json`
