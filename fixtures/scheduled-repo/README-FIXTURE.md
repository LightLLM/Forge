# scheduled-repo fixture

Deterministic workspace for M9 scheduled engineering analyses:

- `todo_analysis` — TODO/FIXME markers
- `dependency_audit` — package.json with `*` range and no lockfile
- `security_scan` — fake api_key string
- `repo_summary` — file metrics
- `dead_code_hints` — unusedExport heuristic
