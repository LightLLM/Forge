# TypeScript skill

1. Fix types at the source; avoid `any` and blanket casts unless unavoidable.
2. Keep public APIs explicit; update callers when signatures change.
3. Run typecheck after edits; treat typecheck failure as incomplete work.
