# M12 — Model Evaluation

## Gate

Fake models compared deterministically via `EvalRunner` + `fixtures/eval/basic.json`.

## Remaining debt

Live provider eval harness; category-specific datasets; orchestrator auto-recording eval metrics.
