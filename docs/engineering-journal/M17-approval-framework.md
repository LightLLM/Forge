# M17 — Human Approval Framework

## Gate

Restricted action cannot execute without explicit approval.

## Evidence

`tests/m17-approvals.test.ts` — pending blocks; model cannot approve; operator can.

## Debt

Not all orchestrator paths (push/deploy) call ApprovalFramework yet — framework API is ready.
