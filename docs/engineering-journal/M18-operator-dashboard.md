# M18 — Operator Dashboard

## Gate

Task state and approvals stay synchronized between CLI and UI.

## Evidence

`tests/m18-dashboard.test.ts` — approve via HTTP; SqliteStore reflects same status.

## Debt

UI is minimal HTML; not a full operator console.
